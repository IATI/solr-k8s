apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls-secret-test
  namespace: default
  annotations:
    issue-temporary-certificate: "true"
spec:
  secretName: solr-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
  commonName: "*.solr-test.iatistandard.org"
  dnsNames:
    - "solr-test.iatistandard.org"
    - "*.solr-test.iatistandard.org"
  keystores:
    pkcs12:
      create: true
      passwordSecretRef:
        key: password-key
        name: pkcs12-password-secret-dev
  subject:
    countries:
      - UK
    organizationalUnits:
      - k8s
